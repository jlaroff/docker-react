language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jlaroff/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jlaroff/docker-react npm run test